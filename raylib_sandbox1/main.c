#include <math.h>
#include <stdio.h>
#include <raylib.h>

//BUILD COMMAND
// clang -framework CoreVideo -framework IOKit -framework Cocoa -framework GLUT -framework OpenGL  libraylib.a example.c -o my_app3

int main()
{
    int yusuf = 12;
    const int screenWidth = 864;
    const int screenHeight = 486;

    InitWindow(screenWidth, screenHeight, "raylib");

    // printf("%s\n", "kenan");
    // return 0;
}
